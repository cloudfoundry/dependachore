---
image_resource:
  type: registry-image
  source:
    repository: cfgarden/garden-ci
    tag: '2.0.24'

platform: linux

inputs:
- name: dependachore

params:
  GCP_SERVICE_ACCOUNT_JSON:

run:
  path: dependachore/ci/deploy-cloud-function.sh
